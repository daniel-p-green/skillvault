# Malicious Skill Fixture

This is an intentionally suspicious fixture bundle used for deterministic golden tests.

- It contains patterns that *look like* network/exec usage.
- v0.1 scanning may not flag these yet; the important part is deterministic hashing + receipts.
